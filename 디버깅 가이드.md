# VS Code C 언어 디버깅 가이드
## Breakpoint를 활용한 디버깅

> **대상**: WSL Ubuntu (강사) 및 macOS (학생)

---

## 🎯 디버깅이란?

**디버깅 (Debugging)**: 프로그램의 오류를 찾아 수정하는 과정

### 일반적인 디버깅 방법
```c
// ❌ 비효율적인 방법
printf("여기까지 왔음\n");
printf("i = %d\n", i);
printf("arr[0] = %d\n", arr[0]);
// ... 수십 개의 printf
```

### Breakpoint 디버깅의 장점
- ✅ 프로그램을 멈추고 변수 상태 확인
- ✅ 한 줄씩 실행하며 흐름 추적
- ✅ 조건부 breakpoint 설정 가능
- ✅ printf 없이도 모든 변수 확인

---

## 📦 필요한 설치 항목

### 1. VS Code 확장 프로그램

#### 필수 확장
**C/C++ (Microsoft)**
- 확장 ID: `ms-vscode.cpptools`
- 기능: IntelliSense, 디버깅, 코드 탐색

**설치 방법**
```
1. VS Code 실행
2. 왼쪽 사이드바에서 Extensions (Ctrl+Shift+X)
3. "C/C++" 검색
4. Microsoft 제공 확장 설치
```

#### 선택 확장 (권장)
**C/C++ Extension Pack**
- 확장 ID: `ms-vscode.cpptools-extension-pack`
- 포함 내용:
  - C/C++
  - C/C++ Themes
  - CMake Tools (나중에 필요)

---

### 2. 디버거 (Debugger)

#### macOS 학생용
**LLDB (기본 설치됨)**

확인 방법:
```bash
lldb --version
```

출력 예시:
```
lldb-1600.0.26.1
Apple Swift version 5.9.2
```

**없다면 설치:**
```bash
xcode-select --install
```

---

#### WSL Ubuntu 강사용
**GDB (GNU Debugger)**

확인 방법:
```bash
gdb --version
```

**설치 (없다면):**
```bash
sudo apt update
sudo apt install gdb
```

출력 예시:
```
GNU gdb (Ubuntu 12.1-0ubuntu1~22.04) 12.1
```

---

### 3. 컴파일러 옵션

**디버깅 정보 포함하여 컴파일**

일반 컴파일:
```bash
clang -o program program.c
```

디버깅용 컴파일:
```bash
clang -g -o program program.c
```

**옵션 설명**
- `-g`: 디버깅 정보를 실행 파일에 포함
- 이 옵션 없이 컴파일하면 breakpoint가 제대로 작동하지 않음!

---

## ⚙️ VS Code 설정

### 1. 프로젝트 폴더 구조

```
my-c-project/
├── .vscode/
│   ├── launch.json      # 디버깅 설정
│   └── tasks.json       # 빌드 작업
├── src/
│   └── main.c
└── README.md
```

---

### 2. launch.json 설정

#### macOS용 (LLDB)

**경로**: `.vscode/launch.json`

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C Debug (lldb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "C: clang build active file"
        }
    ]
}
```

---

#### WSL Ubuntu용 (GDB)

**경로**: `.vscode/launch.json`

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C Debug (gdb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "C: clang build active file"
        }
    ]
}
```

---

### 3. tasks.json 설정

#### macOS용

**경로**: `.vscode/tasks.json`

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C: clang build active file",
            "command": "/usr/bin/clang",
            "args": [
                "-g",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "디버깅 정보를 포함하여 컴파일"
        }
    ]
}
```

---

#### WSL Ubuntu용

**경로**: `.vscode/tasks.json`

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C: clang build active file",
            "command": "/usr/bin/clang",
            "args": [
                "-g",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${file}",
                "-lm"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "디버깅 정보를 포함하여 컴파일 (math 라이브러리 포함)"
        }
    ]
}
```

---

## 🔧 자동 설정 방법 (추천)

**VS Code가 자동으로 설정 파일 생성**

### 방법 1: C/C++ 확장 기능 사용

1. C 파일 열기 (예: `main.c`)
2. 실행 및 디버그 탭 (Ctrl+Shift+D)
3. "launch.json 파일 만들기" 클릭
4. "C++ (GDB/LLDB)" 선택
5. 환경 선택:
   - macOS → LLDB
   - Linux (WSL) → GDB

---

### 방법 2: 명령 팔레트 사용

1. `Ctrl+Shift+P` (macOS: `Cmd+Shift+P`)
2. "C/C++: Add Debug Configuration" 입력
3. 적절한 템플릿 선택

---

## 🎮 디버깅 사용법

### 1. Breakpoint 설정

**방법 1: 마우스 클릭**
- 줄 번호 왼쪽 클릭 → 빨간 점 생성

**방법 2: 단축키**
- 커서를 해당 줄에 놓고 `F9`

**예제 코드**
```c
#include <stdio.h>

int main(void) {
    int arr[5] = {1, 2, 3, 4, 5};
    int sum = 0;
    
    for (int i = 0; i < 5; i++) {    // ← Breakpoint 설정
        sum += arr[i];
        printf("i=%d, sum=%d\n", i, sum);
    }
    
    printf("Total: %d\n", sum);
    return 0;
}
```

---

### 2. 디버깅 시작

**방법 1: 메뉴**
- 실행 → 디버깅 시작 (F5)

**방법 2: 단축키**
- `F5`

**방법 3: UI**
- 왼쪽 사이드바 "실행 및 디버그" → 재생 버튼

---

### 3. 디버깅 컨트롤

**주요 단축키**

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `F5` | Continue | 다음 breakpoint까지 실행 |
| `F10` | Step Over | 현재 줄 실행 (함수는 건너뜀) |
| `F11` | Step Into | 함수 안으로 들어감 |
| `Shift+F11` | Step Out | 함수 밖으로 나옴 |
| `Ctrl+Shift+F5` | Restart | 디버깅 재시작 |
| `Shift+F5` | Stop | 디버깅 중단 |

---

### 4. 변수 확인

디버깅 중 왼쪽 패널에서 확인 가능:

**Variables (변수)**
- 로컬 변수: 현재 함수의 변수들
- 전역 변수: 프로그램 전체 변수들

**Watch (감시)**
- 특정 변수나 표현식 추가하여 계속 확인
- 예: `arr[i]`, `sum`, `i * 2`

**Call Stack (호출 스택)**
- 함수 호출 순서 확인

**Breakpoints (중단점)**
- 설정된 breakpoint 목록

---

### 5. 고급 기능

#### 조건부 Breakpoint

**사용법**
1. Breakpoint 우클릭
2. "조건부 중단점 편집..." 선택
3. 조건 입력 (예: `i == 3`)

**예제**
```c
for (int i = 0; i < 100; i++) {
    sum += arr[i];  // ← i == 50일 때만 멈춤
}
```

---

#### Logpoint

printf 없이 값 출력

**사용법**
1. Breakpoint 우클릭
2. "Logpoint 편집..." 선택
3. 메시지 입력 (예: `i = {i}, sum = {sum}`)

**장점**
- 코드 수정 없이 값 확인
- 프로그램이 멈추지 않음

---

## 📝 실습 예제

### 예제 1: 배열 디버깅

**파일**: `debug_array.c`

```c
#include <stdio.h>

int main(void) {
    int scores[5] = {85, 92, 78, 95, 88};
    int sum = 0;
    int max = scores[0];
    
    // Breakpoint 1: 여기에 설정
    for (int i = 0; i < 5; i++) {
        sum += scores[i];
        
        // Breakpoint 2: 여기에 설정
        if (scores[i] > max) {
            max = scores[i];
        }
    }
    
    printf("합계: %d\n", sum);
    printf("최고점: %d\n", max);
    
    return 0;
}
```

**디버깅 연습**
1. for 루프 시작 부분에 breakpoint
2. F5로 시작
3. F10으로 한 줄씩 실행
4. Variables에서 `i`, `sum`, `scores[i]` 값 확인
5. `i == 2`일 때 조건부 breakpoint 설정

---

### 예제 2: 함수 디버깅

**파일**: `debug_function.c`

```c
#include <stdio.h>

int factorial(int n) {
    int result = 1;
    
    // Breakpoint 3: 함수 내부
    for (int i = 1; i <= n; i++) {
        result *= i;
    }
    
    return result;
}

int main(void) {
    int num = 5;
    
    // Breakpoint 1: 함수 호출 전
    int fact = factorial(num);
    
    // Breakpoint 2: 함수 호출 후
    printf("%d! = %d\n", num, fact);
    
    return 0;
}
```

**디버깅 연습**
1. main의 `factorial(num)` 줄에 breakpoint
2. F11로 함수 안으로 들어가기 (Step Into)
3. 함수 내부에서 변수 확인
4. Shift+F11로 함수 밖으로 나오기 (Step Out)
5. Call Stack에서 함수 호출 순서 확인

---

## 🐛 자주 발생하는 문제

### 문제 1: Breakpoint가 회색으로 표시됨

**원인**: `-g` 옵션 없이 컴파일

**해결책**
```bash
# 기존
clang -o program program.c

# 올바름
clang -g -o program program.c
```

---

### 문제 2: "Unable to start debugging"

**원인**: launch.json 설정 오류

**해결책**
1. `.vscode/launch.json` 확인
2. `program` 경로 확인
3. 컴파일된 실행 파일 존재 확인

---

### 문제 3: 변수 값이 "optimized out"으로 표시

**원인**: 최적화 옵션 사용

**해결책**
```bash
# 최적화 끄기
clang -g -O0 -o program program.c
```

---

### 문제 4: macOS에서 "Unable to resolve non-existing file"

**원인**: 경로 문제

**해결책**
```json
// launch.json에서
"program": "${fileDirname}/${fileBasenameNoExtension}",
// 대신
"program": "${workspaceFolder}/실행파일이름"
```

---

## 📚 디버깅 베스트 프랙티스

### 1. 전략적 Breakpoint 설정

**좋은 위치**
- 반복문 시작 부분
- 조건문 분기점
- 함수 호출 전후
- 의심스러운 계산 부분

**나쁜 위치**
- 단순 선언문
- 주석
- 함수 끝 (return 후)

---

### 2. Step Over vs Step Into

**Step Over (F10)**
- 함수를 건너뛰고 다음 줄로
- 이미 검증된 함수 호출 시

**Step Into (F11)**
- 함수 안으로 들어감
- 버그가 있을 것 같은 함수

---

### 3. Watch 활용

**유용한 표현식**
```
arr[i]              // 배열 요소
i < size            // 조건식
arr[i] * 2          // 계산식
&variable           // 주소 (포인터 학습 후)
```

---

### 4. 조건부 Breakpoint 활용

**예제**
```c
// 배열 크기가 100일 때
for (int i = 0; i < 100; i++) {
    process(arr[i]);  // ← i == 95일 때만 확인하고 싶다면
}

// 조건: i == 95
```

---

## 🎓 학생들을 위한 가이드

### 수업 시간 실습

**단계별 진행**
1. 강사가 예제 코드 공유
2. 학생들이 breakpoint 설정
3. 함께 디버깅하며 변수 추적
4. 각자 다른 조건으로 실험

---

### 과제에 활용

**디버깅 보고서 양식**
```
문제: 배열 최대값 찾기

버그 발견 과정:
1. Breakpoint 위치: 15번 줄 (if문)
2. 변수 상태: max = 0, arr[i] = 85
3. 발견한 문제: max 초기값이 0으로 잘못 설정됨
4. 해결: max = arr[0]로 수정

스크린샷:
[Variables 창 캡처]
```

---

## 💡 추가 팁

### VS Code 설정 추천

**settings.json**
```json
{
    "C_Cpp.debugShortcut": true,
    "C_Cpp.errorSquiggles": "enabled",
    "debug.console.fontSize": 14,
    "debug.console.wordWrap": true
}
```

---

### 터미널에서 GDB 직접 사용 (심화)

```bash
# 컴파일
clang -g -o program program.c

# GDB 실행
gdb ./program

# GDB 명령어
(gdb) break main          # main에 breakpoint
(gdb) run                 # 실행
(gdb) next                # Step Over
(gdb) step                # Step Into
(gdb) print variable      # 변수 출력
(gdb) continue            # 계속 실행
(gdb) quit                # 종료
```

---

## 📖 참고 자료

### 공식 문서
- **VS Code C/C++ Debugging**: https://code.visualstudio.com/docs/cpp/cpp-debug
- **GDB Documentation**: https://www.gnu.org/software/gdb/documentation/
- **LLDB Tutorial**: https://lldb.llvm.org/use/tutorial.html

### 단축키 치트시트

| 작업 | Windows/Linux | macOS |
|------|---------------|-------|
| 디버그 시작 | F5 | F5 |
| Step Over | F10 | F10 |
| Step Into | F11 | F11 |
| Step Out | Shift+F11 | Shift+F11 |
| Breakpoint 토글 | F9 | F9 |
| 명령 팔레트 | Ctrl+Shift+P | Cmd+Shift+P |

---

## ✅ 체크리스트

**설치 확인**
- [ ] VS Code C/C++ 확장 설치
- [ ] 디버거 설치 확인 (GDB 또는 LLDB)
- [ ] 컴파일러 동작 확인 (clang 또는 gcc)

**설정 확인**
- [ ] `.vscode/launch.json` 생성
- [ ] `.vscode/tasks.json` 생성
- [ ] `-g` 옵션으로 컴파일 가능

**기능 테스트**
- [ ] Breakpoint 설정/해제
- [ ] F5로 디버깅 시작
- [ ] F10/F11로 Step Over/Into
- [ ] Variables에서 변수 확인
- [ ] Watch에 표현식 추가

---

**다음 단계**: 실제 버그가 있는 코드로 디버깅 실습!