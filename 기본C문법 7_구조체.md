# Session 07 - 구조체 (struct)

---

## 1. 문제 먼저 생각하기

학생 3명의 이름, 학번, 점수를 저장하는 프로그램을 만들어야 한다고 가정하자.

```c
// 지금까지 배운 방법으로 작성한다면?
char  name1[20], name2[20], name3[20];
int   id1, id2, id3;
float score1, score2, score3;
```

학생이 100명이라면? 변수 **300개**를 선언해야 한다.  
배열로 해결하려 해도, **이름·학번·점수가 서로 다른 타입**이라 하나의 배열에 담을 수 없다.

> **핵심 질문:** 서로 다른 타입의 데이터를 하나로 묶어서 관리할 수 있을까?

---

## 2. 구조체(struct)란?

**서로 다른 타입의 데이터를 하나로 묶은 사용자 정의 타입**이다.

```
struct Student
┌──────────────────────────────┐
│  char  name[20]   (20 bytes) │
│  int   id         ( 4 bytes) │
│  float score      ( 4 bytes) │
└──────────────────────────────┘
```

---

## 3. 왜 typedef가 따로 필요할까?

처음 구조체를 배우면 자연스럽게 이런 의문이 생긴다.

> "그냥 `struct Student { ... };` 하나만 쓰면 되지, 왜 `typedef`까지 써야 하지?"

### 3.1 C의 이름 공간(namespace) 분리

C에서는 이름을 저장하는 공간이 **두 개** 분리되어 있다.

| 이름 공간 | 해당되는 것 |
|-----------|------------|
| **태그(tag) 공간** | `struct`, `union`, `enum` 이름 |
| **일반 식별자 공간** | 변수, 함수, `typedef` 이름 |

`struct Student`를 정의하면 `Student`라는 이름은 **태그 공간에만** 등록된다.  
일반 변수를 선언하는 **일반 식별자 공간**에는 없기 때문에, 아래와 같은 현상이 생긴다.

```c
struct Student { int id; float score; };

Student s1;         // ❌ 오류: 일반 식별자 공간에는 'Student'가 없음
struct Student s1;  // ✅ 태그 공간임을 'struct' 키워드로 명시해야 함
```

`typedef`는 태그 공간의 이름을 **일반 식별자 공간으로 복사**하는 역할을 한다.

```c
typedef struct { int id; float score; } Student;

Student s1;  // ✅ 이제 일반 식별자 공간에 'Student'가 있으므로 정상
```

### 3.2 왜 이렇게 설계했나?

C는 1972년 Dennis Ritchie가 설계했으며, 핵심 철학은 **"언어는 최소한의 규칙만 갖는다"** 였다.  
이름 공간을 분리하면 아래와 같이 동일한 이름을 두 공간에서 **충돌 없이** 사용할 수 있다.

```c
struct Student { int id; };   // 태그 공간의 'Student'

int Student = 5;              // ✅ C에서는 가능 — 일반 식별자 공간의 'Student'
                              // 두 공간이 분리되어 있으므로 충돌하지 않음
```

언어 규칙을 단순하게 유지하면서 이름 충돌 가능성을 최소화하려는 의도였다.

### 3.3 C++는 이 불편함을 고쳤다

C의 이 설계가 불편하다는 피드백을 반영해, C++에서는 `struct` 선언만으로  
자동으로 일반 식별자 공간에도 등록되도록 개선되었다.

```cpp
// C++에서는 typedef 없이도 동작
struct Student { int id; float score; };
Student s1;  // ✅ C++에서는 정상
```

> **정리:** C에서 `typedef`가 필요한 것은 버그가 아니라,  
> 이름 공간 분리라는 **의도적 설계** 때문이다.  
> C++는 이 불편함을 인정하고 자동화했다.

*참고: Ritchie, D.M. (1993). The Development of the C Language. ACM SIGPLAN Notices, 28(3), 201–208.*

---

## 4. 구조체 정의와 변수 선언

### 4.1 기본 문법

```c
// 구조체 타입 정의 (설계도 작성)
struct Student {
    char  name[20];
    int   id;
    float score;
};

// 구조체 변수 선언 (실제 메모리 할당)
struct Student s1;
struct Student s2;
```

> **주의:** 구조체 정의는 **설계도**일 뿐이다. 실제 메모리는 변수를 선언할 때 할당된다.

### 4.2 typedef로 간편하게 사용하기

매번 `struct Student`라고 쓰는 것은 불편하다. `typedef`를 사용하면 타입 이름을 짧게 만들 수 있다.

```c
typedef struct {
    char  name[20];
    int   id;
    float score;
} Student;   // 이제 'Student'만으로 사용 가능

Student s1;  // struct Student s1; 과 동일
Student s2;
```

### 4.3 선언과 동시에 초기화

```c
Student s1 = {"홍길동", 20240001, 88.5f};
Student s2 = {.name = "이순신", .id = 20240002, .score = 95.0f};  // 지정 초기화
```

---

## 5. 멤버 접근: `.` 연산자

구조체의 각 데이터에 접근할 때는 **점(`.`) 연산자**를 사용한다.

```c
#include <stdio.h>
#include <string.h>

typedef struct {
    char  name[20];
    int   id;
    float score;
} Student;

int main(void) {
    Student s1;

    // 멤버에 값 대입
    strcpy(s1.name, "홍길동");   // 문자열은 strcpy 사용 (= 직접 대입 불가)
    s1.id    = 20240001;
    s1.score = 88.5f;

    // 멤버 값 출력
    printf("이름: %s\n",  s1.name);
    printf("학번: %d\n",  s1.id);
    printf("점수: %.1f\n", s1.score);

    return 0;
}
```

**실행 결과:**
```
이름: 홍길동
학번: 20240001
점수: 88.5
```

> **왜 `s1.name = "홍길동"`은 안 될까?**  
> `name`은 배열이다. 배열은 포인터처럼 동작하기 때문에 `=`로 직접 문자열을 복사할 수 없다.  
> 반드시 `strcpy()`를 사용해야 한다. (`#include <string.h>` 필요)

---

## 6. 구조체의 메모리 구조

```
Student s1 의 메모리 (총 28 bytes*)

주소(예시)  내용
─────────────────────────────────────────────────
0x100       name[0] ~ name[19]   (20 bytes) "홍길동\0..."
0x114       id                   ( 4 bytes) 20240001
0x118       score                ( 4 bytes) 88.5
─────────────────────────────────────────────────
```

> \* 컴파일러에 따라 **정렬(padding)** 때문에 실제 크기가 달라질 수 있다.  
> `sizeof(Student)`로 확인하는 것이 정확하다.

---

## 7. 구조체와 함수

### 7.1 값으로 전달 (Call by Value)

```c
void print_student(Student s) {   // 구조체 전체가 복사됨
    printf("[%d] %s - %.1f점\n", s.id, s.name, s.score);
}

int main(void) {
    Student s1 = {"홍길동", 20240001, 88.5f};
    print_student(s1);   // s1의 복사본이 전달됨
    return 0;
}
```

**문제점:** 구조체가 크면 복사 비용이 크다.  
멤버 수십 개짜리 구조체를 매번 복사하면 메모리와 시간이 낭비된다.

### 7.2 포인터로 전달 (권장)

```c
void print_student(Student *p) {   // 주소만 전달 (8 bytes)
    printf("[%d] %s - %.1f점\n", p->id, p->name, p->score);
}

int main(void) {
    Student s1 = {"홍길동", 20240001, 88.5f};
    print_student(&s1);   // 주소 전달
    return 0;
}
```

> **`->` 연산자:** 구조체 **포인터**로 멤버에 접근할 때 사용한다.  
> `p->id`  는  `(*p).id`  와 동일하다.

| 상황 | 연산자 | 예시 |
|------|--------|------|
| 구조체 변수 직접 접근 | `.` | `s1.id` |
| 구조체 포인터로 접근 | `->` | `p->id` |

### 7.3 구조체 수정 함수

함수 안에서 구조체 값을 변경하려면 반드시 포인터를 전달해야 한다.

```c
void add_bonus(Student *p, float bonus) {
    p->score += bonus;   // 원본 수정
}

int main(void) {
    Student s1 = {"홍길동", 20240001, 88.5f};
    printf("수정 전: %.1f\n", s1.score);  // 88.5

    add_bonus(&s1, 5.0f);
    printf("수정 후: %.1f\n", s1.score);  // 93.5
    return 0;
}
```

---

## 8. 구조체 배열

```c
#include <stdio.h>
#include <string.h>

typedef struct {
    char  name[20];
    int   id;
    float score;
} Student;

int main(void) {
    Student class[3] = {
        {"홍길동", 20240001, 88.5f},
        {"이순신", 20240002, 95.0f},
        {"강감찬", 20240003, 72.0f}
    };

    // 배열 순회
    for (int i = 0; i < 3; i++) {
        printf("[%d] %s - %.1f점\n", class[i].id, class[i].name, class[i].score);
    }

    return 0;
}
```

**실행 결과:**
```
[20240001] 홍길동 - 88.5점
[20240002] 이순신 - 95.0점
[20240003] 강감찬 - 72.0점
```

---

## 9. 구조체 배열과 포인터

```c
void print_all(Student *arr, int n) {
    for (int i = 0; i < n; i++) {
        printf("[%d] %s - %.1f점\n", arr[i].id, arr[i].name, arr[i].score);
        // 또는: (arr + i)->id, (arr + i)->name ...
    }
}

int main(void) {
    Student class[3] = {
        {"홍길동", 20240001, 88.5f},
        {"이순신", 20240002, 95.0f},
        {"강감찬", 20240003, 72.0f}
    };

    print_all(class, 3);   // 배열 이름 = 첫 번째 원소의 주소
    return 0;
}
```

---

## 10. 핵심 정리

| 개념 | 설명 |
|------|------|
| `struct` | 서로 다른 타입의 데이터를 하나로 묶는 사용자 정의 타입 |
| `typedef` | 타입에 별칭을 붙여 `struct` 키워드 없이 사용 가능 |
| `.` 연산자 | 구조체 **변수**의 멤버 접근 |
| `->` 연산자 | 구조체 **포인터**의 멤버 접근 (`(*p).x`와 동일) |
| `strcpy()` | 구조체 내 `char` 배열에 문자열 복사 시 사용 |
| 포인터 전달 | 크기가 큰 구조체를 함수에 전달할 때 권장 |

---

## 11. 연습 문제

### 문제 1 (기본)
아래 조건에 맞는 구조체를 정의하고, 변수 1개를 선언하여 값을 출력하라.

- 구조체 이름: `Point`
- 멤버: `x` (int), `y` (int)
- 출력 형식: `(3, 7)`

---

### 문제 2 (중급)
학생 5명의 점수를 구조체 배열로 저장하고, 평균 점수를 출력하는 함수를 작성하라.

```c
typedef struct {
    char  name[20];
    float score;
} Student;

// 아래 함수를 완성하라
float calc_average(Student *arr, int n) {
    // TODO
}
```

---

### 문제 3 (심화)
아래 함수를 완성하라.  
조건: 포인터를 사용해 구조체 배열에서 점수가 가장 높은 학생을 찾아 반환한다.

```c
Student *find_top(Student *arr, int n) {
    // TODO: 가장 높은 점수의 학생 포인터를 반환
}
```

`main`에서 `find_top`의 반환값을 받아 해당 학생의 이름과 점수를 출력하라.

---

*참고: ISO/IEC 9899:2011 (C11) §6.7.2.1 Structure and union specifiers*
