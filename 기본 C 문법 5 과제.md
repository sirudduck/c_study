# C Programming Session 6: 과제

**제출 마감**: 다음 수업 시작 전까지  
**제출 방법**: 각 과제를 별도의 `.c` 파일로 작성하여 제출

---

## 과제 1: 문자열 뒤집기 ⭐⭐

**학습 목표:**
- 포인터로 문자열 조작
- 두 포인터 기법 (양 끝에서 중간으로)
- swap 활용

**요구사항:**
- 문자열을 제자리에서 뒤집기 (in-place reversal)
- 포인터 사용
- 새로운 배열 생성 금지

**코드 템플릿:**
```c
#include <stdio.h>

// 두 문자를 교환하는 함수
void swap_char(char *a, char *b) {
    // 여기에 코드 작성
}

// 문자열 길이 구하기
int my_strlen(char *str) {
    // 여기에 코드 작성
}

// 문자열 뒤집기
void reverse_string(char *str) {
    // 여기에 코드 작성
    // 힌트:
    // 1. 문자열 길이 구하기
    // 2. 시작 포인터 (start)와 끝 포인터 (end) 설정
    // 3. start < end 동안:
    //    - *start와 *end 교환
    //    - start++, end--
}

int main(void) {
    char str1[] = "Hello";
    char str2[] = "Programming";
    char str3[] = "A";
    char str4[] = "";
    
    printf("Original: %s\n", str1);
    reverse_string(str1);
    printf("Reversed: %s\n\n", str1);
    
    printf("Original: %s\n", str2);
    reverse_string(str2);
    printf("Reversed: %s\n\n", str2);
    
    printf("Original: %s\n", str3);
    reverse_string(str3);
    printf("Reversed: %s\n\n", str3);
    
    printf("Original: %s\n", str4);
    reverse_string(str4);
    printf("Reversed: %s\n", str4);
    
    return 0;
}
```

**목표 출력:**
```
Original: Hello
Reversed: olleH

Original: Programming
Reversed: gnimmargorP

Original: A
Reversed: A

Original: 
Reversed: 
```

**테스트 케이스:**
- 짝수 길이 문자열
- 홀수 길이 문자열
- 길이 1인 문자열
- 빈 문자열

---

## 과제 2: 문자열에서 특정 문자 제거 ⭐⭐⭐

**학습 목표:**
- 포인터로 문자열 조작
- 제자리 알고리즘 (in-place algorithm)
- 문자열 압축

**요구사항:**
- 문자열에서 특정 문자를 모두 제거
- 제자리에서 수정 (새 배열 생성 금지)
- 포인터 사용

**코드 템플릿:**
```c
#include <stdio.h>

void remove_char(char *str, char c) {
    // 여기에 코드 작성
    // 힌트:
    // 1. 읽기 포인터와 쓰기 포인터 사용
    // 2. 읽기 포인터로 문자열 순회
    // 3. c가 아닌 문자만 쓰기 포인터 위치에 복사
    // 4. 마지막에 null terminator 추가
}

int main(void) {
    char str1[] = "Hello World";
    char str2[] = "Programming";
    char str3[] = "aaaaaa";
    
    printf("Original: %s\n", str1);
    remove_char(str1, 'l');
    printf("After removing 'l': %s\n\n", str1);
    
    printf("Original: %s\n", str2);
    remove_char(str2, 'r');
    printf("After removing 'r': %s\n\n", str2);
    
    printf("Original: %s\n", str3);
    remove_char(str3, 'a');
    printf("After removing 'a': %s\n", str3);
    
    return 0;
}
```

**목표 출력:**
```
Original: Hello World
After removing 'l': Heo Word

Original: Programming
After removing 'r': Pogamming

Original: aaaaaa
After removing 'a': 
```

---

## 과제 3: 두 문자열 비교 (대소문자 무시) ⭐⭐⭐

**학습 목표:**
- 문자열 비교 알고리즘
- 대소문자 변환
- 포인터 활용

**요구사항:**
- 두 문자열을 비교하되 대소문자 무시
- 반환값: 같으면 0, s1 < s2면 음수, s1 > s2면 양수
- `<ctype.h>`의 `tolower()` 함수 사용 가능

**코드 템플릿:**
```c
#include <stdio.h>
#include <ctype.h>  // tolower() 함수

int strcasecmp(char *s1, char *s2) {
    // 여기에 코드 작성
    // 힌트:
    // 1. 두 문자열을 동시에 순회
    // 2. 각 문자를 소문자로 변환하여 비교
    // 3. 다르면 그 차이 반환
    // 4. 끝까지 같으면 0 반환
}

int main(void) {
    printf("%d\n", strcasecmp("Hello", "hello"));    // 0
    printf("%d\n", strcasecmp("Hello", "HELLO"));    // 0
    printf("%d\n", strcasecmp("abc", "ABC"));        // 0
    printf("%d\n", strcasecmp("abc", "abd"));        // 음수
    printf("%d\n", strcasecmp("abd", "abc"));        // 양수
    printf("%d\n", strcasecmp("Apple", "APPLY"));    // 음수
    
    return 0;
}
```

**참고**: `tolower()` 함수는 문자를 소문자로 변환합니다.
```c
char c = 'A';
char lower = tolower(c);  // 'a'
```

---

## 과제 4: 선택 정렬 구현 (포인터 버전) ⭐⭐⭐⭐

**학습 목표:**
- 정렬 알고리즘 이해
- 포인터로 배열 조작
- 최솟값 찾기 알고리즘

**요구사항:**
- 선택 정렬로 배열 오름차순 정렬
- 포인터 사용
- swap 함수 활용

**선택 정렬 알고리즘:**
1. 배열에서 최솟값을 찾음
2. 최솟값을 맨 앞과 교환
3. 나머지 부분에 대해 반복

```
예시: [64, 25, 12, 22, 11]

1회전: 최솟값 11 찾아서 64와 교환
→ [11, 25, 12, 22, 64]

2회전: 나머지에서 최솟값 12 찾아서 25와 교환
→ [11, 12, 25, 22, 64]

3회전: 나머지에서 최솟값 22 찾아서 25와 교환
→ [11, 12, 22, 25, 64]

4회전: 나머지에서 최솟값 25, 이미 제자리
→ [11, 12, 22, 25, 64]

완료!
```

**코드 템플릿:**
```c
#include <stdio.h>

void swap(int *a, int *b) {
    // swap 함수 구현
}

// 최솟값의 포인터를 반환하는 함수
int* find_min(int *start, int *end) {
    // 여기에 코드 작성
    // start부터 end 전까지 범위에서 최솟값의 주소 반환
    // 힌트:
    // 1. min_ptr = start로 초기화
    // 2. start+1부터 end까지 순회
    // 3. *ptr < *min_ptr이면 min_ptr 업데이트
    // 4. min_ptr 반환
}

void selection_sort(int arr[], int size) {
    // 여기에 코드 작성
    // 힌트:
    // for (int *i = arr; i < arr + size - 1; i++) {
    //     int *min_ptr = find_min(i, arr + size);
    //     swap(i, min_ptr);
    // }
}

void print_array(int arr[], int size) {
    printf("[");
    for (int i = 0; i < size; i++) {
        printf("%d", arr[i]);
        if (i < size - 1) printf(", ");
    }
    printf("]\n");
}

int main(void) {
    int arr[] = {64, 25, 12, 22, 11};
    int size = 5;
    
    printf("정렬 전: ");
    print_array(arr, size);
    
    selection_sort(arr, size);
    
    printf("정렬 후: ");
    print_array(arr, size);
    
    return 0;
}
```

**목표 출력:**
```
정렬 전: [64, 25, 12, 22, 11]
정렬 후: [11, 12, 22, 25, 64]
```

**도전 과제 (선택):**
정렬 과정을 단계별로 출력하는 기능 추가

---

## 과제 5: 문자열 토큰화 (strtok 구현) ⭐⭐⭐⭐⭐

**학습 목표:**
- 고급 문자열 처리
- 포인터 조작
- 함수 상태 유지

**요구사항:**
- 문자열을 구분자로 나누기
- 공백으로 구분된 단어 추출
- 포인터 사용

**동작 방식:**
```c
char str[] = "Hello World Programming";
첫 호출: "Hello" 반환
두 번째: "World" 반환
세 번째: "Programming" 반환
네 번째: NULL 반환
```

**코드 템플릿:**
```c
#include <stdio.h>

// 문자열에서 다음 토큰을 찾아 반환
// 구분자를 '\0'으로 바꾸고 다음 토큰의 시작 위치 반환
char* next_token(char *str, char delimiter) {
    // 여기에 코드 작성
    // 힌트:
    // 1. static 변수로 현재 위치 저장
    // 2. str이 NULL이 아니면 새로운 문자열 시작
    // 3. 현재 위치에서 delimiter 찾기
    // 4. delimiter를 '\0'으로 바꾸기
    // 5. 토큰 시작 위치 반환, 다음 위치 저장
}

int main(void) {
    char str[] = "Hello World Programming C";
    char *token;
    
    token = next_token(str, ' ');
    while (token != NULL) {
        printf("%s\n", token);
        token = next_token(NULL, ' ');
    }
    
    return 0;
}
```

**목표 출력:**
```
Hello
World
Programming
C
```

**힌트**: `static` 변수를 사용하여 함수 호출 간 상태 유지

```c
char* next_token(char *str, char delimiter) {
    static char *current = NULL;  // 함수 호출 간 유지됨
    
    if (str != NULL) {
        current = str;  // 새 문자열 시작
    }
    
    // ... 나머지 코드
}
```

---

## 제출 시 주의사항

### 파일명 규칙
```
과제1: homework6_1.c
과제2: homework6_2.c
과제3: homework6_3.c
과제4: homework6_4.c
과제5: homework6_5.c (선택)
```

### 코드 작성 규칙
1. **포인터 사용 필수**: 배열 인덱스 대신 포인터 사용
2. **주석 작성**: 알고리즘 설명 주석 추가
3. **변수명**: 의미 있는 이름 사용
4. **테스트**: 여러 테스트 케이스로 검증

### 채점 기준
- **동작 정확성** (40%): 요구사항대로 작동하는가?
- **포인터 활용** (30%): 포인터를 올바르게 사용했는가?
- **코드 품질** (20%): 읽기 쉽고 효율적인가?
- **엣지 케이스** (10%): 특수한 경우도 처리하는가?

---

## 도움말

### 자주하는 실수

**1. 배열 이름 증가 시도**
```c
// Wrong
char str[] = "Hello";
str++;  // 컴파일 오류!

// Correct
char *ptr = str;
ptr++;  // OK
```

**2. 포인터 초기화 안 함**
```c
// Wrong
char *ptr;
while (*ptr != '\0') { ... }  // 정의되지 않은 동작!

// Correct
char *ptr = str;
while (*ptr != '\0') { ... }
```

**3. null terminator 잊어버림**
```c
// Wrong
void my_strcpy(char *dest, char *src) {
    while (*src != '\0') {
        *dest++ = *src++;
    }
    // '\0' 안 복사함!
}

// Correct
void my_strcpy(char *dest, char *src) {
    while (*src != '\0') {
        *dest++ = *src++;
    }
    *dest = '\0';  // 필수!
}
```

**4. 문자열 리터럴 수정**
```c
// Wrong
char *str = "Hello";
str[0] = 'h';  // 세그먼테이션 오류!

// Correct
char str[] = "Hello";
str[0] = 'h';  // OK
```

### 디버깅 팁

**포인터 위치 확인:**
```c
printf("ptr = %p, *ptr = %c\n", (void*)ptr, *ptr);
```

**배열 내용 확인:**
```c
void print_string_debug(char *str) {
    printf("String: [");
    while (*str != '\0') {
        printf("'%c'(%d) ", *str, *str);
        str++;
    }
    printf("'\\0']\n");
}
```

**포인터 범위 확인:**
```c
int arr[5];
int *start = arr;
int *end = arr + 5;

for (int *ptr = start; ptr < end; ptr++) {
    printf("ptr - start = %ld\n", ptr - start);
}
```

---

## 참고 자료

### 공식 문서
- [ISO C Standard - Arrays](https://www.open-std.org/jtc1/sc22/wg14/www/docs/n1570.pdf)
- [ISO C Standard - String Functions](https://www.open-std.org/jtc1/sc22/wg14/www/docs/n1570.pdf) Section 7.24

### 권장 자료
- Kernighan & Ritchie, "The C Programming Language", Chapter 5
- [Stanford Pointers and Arrays](http://cslibrary.stanford.edu/106/)

---

## 선택 과제 (보너스)

### 보너스 1: 문자열 압축

연속된 같은 문자를 개수로 표현

```c
입력: "aaabbcccc"
출력: "a3b2c4"

입력: "abc"
출력: "abc" (압축 안 됨)
```

### 보너스 2: 회문 검사

문자열이 회문인지 확인 (포인터 사용)

```c
입력: "radar"
출력: 1 (회문)

입력: "hello"
출력: 0 (회문 아님)
```

---

**질문이 있으면 수업 시간에 또는 이메일로 문의하세요!**

**행운을 빕니다! 🚀**
